{
  "name": "source-code-tooling",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
        "check": "tsc --noEmit",
        "start": "bun run src/snake-to-pascal.ts",
        "test": "bun test src/snake-to-pascal.test.ts",
        "dev": "bun run check && bun run test && bun run start"
    },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "peerDependencies": {
    "typescript": "^7.0.1-rc"
  }
}
